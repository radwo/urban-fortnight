version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: s1-radwo
blocks:
  - name: Build & Test
    task:
      jobs:
        - name: Test
          commands:
            - ls -lia
            - echo "2"
