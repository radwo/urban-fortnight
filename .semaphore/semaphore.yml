version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e2-standard-2
    os_image: ubuntu2004
blocks:
  - name: Build & Test
    task:
      jobs:
        - name: Test
          commands:
            - checkout
            - ls -lia
            - env
      secrets:
        - name: organization
